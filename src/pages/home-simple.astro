---
---
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Home - RKT Regulador</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f4; }
        .header { background: #333; color: #fff; padding: 1rem; }
        .header-content { display: flex; justify-content: space-between; align-items: center; max-width: 1200px; margin: 0 auto; }
        .logo h2 { margin: 0; font-size: 1.2rem; }
        .nav { display: flex; gap: 1rem; }
        .nav a, .nav button { color: white; text-decoration: none; padding: 0.5rem; background: none; border: none; cursor: pointer; }
        .nav a:hover, .nav button:hover { background: rgba(255,255,255,0.1); }
        main { max-width: 1200px; margin: 2rem auto; padding: 1rem; background-color: white; border-radius: 8px; }
        .hidden { display: none; }
    </style>
</head>
<body>
    <header class="header">
        <div class="header-content">
            <div class="logo">
                <h2>Gestión Almacén Regulador</h2>
            </div>
            <nav class="nav" id="nav" style="display: none;">
                <a href="/home">Home</a>
                <a href="/pedidos/subir">Subir pedido</a>
                <a href="/pedidos">Ver pedidos</a>
                <a href="/usuarios">Usuarios</a>
                <button onclick="logout()">Salir</button>
            </nav>
        </div>
    </header>

    <main>
        <div id="loading">Verificando acceso...</div>
        <div id="content" class="hidden">
            <h3>Sistema de Gestión de Pedidos para Almacenes Reguladores de RIKUTEC</h3>
        </div>
        <div id="no-auth" class="hidden">
            <h3>Acceso no autorizado</h3>
            <p>Debe iniciar sesión para acceder a esta página.</p>
            <a href="/">Ir al Login</a>
        </div>
    </main>

    <script>
        function logout() {
            localStorage.removeItem('rkt-session');
            window.location.href = '/';
        }

        // Verificación simple
        const session = localStorage.getItem('rkt-session');
        const loading = document.getElementById('loading');
        const content = document.getElementById('content');
        const noAuth = document.getElementById('no-auth');
        const nav = document.getElementById('nav');

        if (session) {
            loading.style.display = 'none';
            content.style.display = 'block';
            nav.style.display = 'flex';
        } else {
            loading.style.display = 'none';
            noAuth.style.display = 'block';
        }
    </script>
</body>
</html>